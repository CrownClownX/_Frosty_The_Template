name: Deploy .NET API to ECS

on:
  push:
    branches: [ "main" ]
    paths: [ "src/Frosty.Api/**", ".github/workflows/api-deploy.yml" ]
  workflow_dispatch:

env:
  AWS_REGION: eu-central-1           # change if needed
  ECR_REPOSITORY: your-api-repo      # created in step 3
  ECS_CLUSTER: your-ecs-cluster      # created in step 3
  ECS_SERVICE: your-ecs-service      # created in step 3
  CONTAINER_NAME: api
  IMAGE_TAG: ${{ github.sha }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write     # required for OIDC
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::457518023943:role/github-deploy
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build & push image
        uses: docker/build-push-action@v6
        with:
          context: ./src/Frosty.Api
          push: true
          tags: ${{ steps.ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}
